import React from "react";
import { DividerWrapper } from "./styled-components/DividerWrapper";

const Divider = () => (
	<React.Fragment>
		<DividerWrapper />
	</React.Fragment>
);

export default Divider;
